/*   <script>
        // Função para verificar a largura da tela e aplicar estilos ou comportamentos diferentes
        function ajustarParaMobileOuWeb() {
            if (window.innerWidth <= 768) {
                // Largura da tela é menor ou igual a 768px (assumindo que seja um dispositivo móvel)
                // Faça adaptações para a versão mobile aqui
                document.body.style.backgroundColor = 'lightblue';
            } else {
                // Largura da tela é maior que 768px (assumindo que seja uma versão web)
                // Mantenha ou aplique adaptações para a versão web aqui
                document.body.style.backgroundColor = 'lightgray';
            }
        }

        // Chame a função para fazer a verificação assim que a página for carregada
        window.onload = ajustarParaMobileOuWeb;

        // Adicione um ouvinte de evento para redimensionamento da janela (caso o usuário gire o dispositivo)
        window.addEventListener('resize', ajustarParaMobileOuWeb);
    </script>*/